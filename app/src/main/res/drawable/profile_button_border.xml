<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Viền -->
    <stroke
        android:width="1dp"
        android:color="#bababa" /> <!-- Màu viền -->

    <!-- Nền (nếu cần) -->
    <solid android:color="@color/alpha" /> <!-- Nền trắng -->

    <!-- Bo góc (nếu muốn) -->
    <corners android:radius="50dp" />
</shape>
